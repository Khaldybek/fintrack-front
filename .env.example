# Копируйте в .env.local для локальной разработки (опционально).
# На Vercel задайте переменные в настройках проекта (Settings → Environment Variables).

# URL бэкенда API. Локально по умолчанию http://localhost:3000
NEXT_PUBLIC_API_URL=http://192.168.8.115:3000
